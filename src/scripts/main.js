'use strict';

$(document).ready(function() {
  $('#select').styler();

  $('.grats__galery-box').lightGallery({
    thumbnail: true,
    // animateThumb: false,
    // showThumbByDefault: false,
  });
});
